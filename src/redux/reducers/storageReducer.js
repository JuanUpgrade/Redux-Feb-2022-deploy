const INITIAL_STATE = {
    post: {},
    comments: {},
};

const storageReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default storageReducer;